# 仓库发布验收

日期：2026-09-06。以下记录本地与线上发布检查结果。

## 本地检查

- 普通相对路径构建通过，`npm run check -- --dist dist` 通过。
- `/qingmingshanghetu/` 子路径构建通过，HTML/CSS 引用及静态资产校验通过。
- 将计划提交的全部文件复制到独立目录，重新 `npm ci`、构建、校验通过；未借用原项目 `node_modules/`。
- 独立副本启动成品 HTTP 服务后，22 个 `public/` 文件逐一读取并比对 SHA-256，全部一致；GLB 与 WASM MIME 类型正确。
- 实际解析 GLB：12.22 MiB，包含 124 人、8 船及网页动态标签。
- 音乐清单中 203 个本地采样校验通过；另在临时目录从固定上游版本下载一个样本，哈希校验通过。
- 音频下载器与 4K 剪辑脚本语法检查通过，剪辑参数帮助与 macOS 启动器语法检查通过。
- 文档内部链接检查通过；待公开文件未发现超限文件、常见凭据格式或原电脑绝对路径。
- GitHub Actions 的 action 提交 SHA 已与官方仓库标签核对。
- 解锁后实际打开待发布网页，三维场景与虹桥简介正常显示，参考图片加载成功，浏览器未记录错误日志。

Three.js 构建块超过 Vite 默认 500 kB 提醒阈值；这是体积提醒，构建完成。网页本身的 UI 与 4K 录像历史验收另见相关文档。

## 远程状态

仓库目标为 `Rising1234Sun/qingmingshanghetu`，用户选择公开。

已发布[公开仓库](https://github.com/Rising1234Sun/qingmingshanghetu)，并启用 [GitHub Pages 在线体验](https://rising1234sun.github.io/qingmingshanghetu/)。

- GitHub 的[构建与资源校验](https://github.com/Rising1234Sun/qingmingshanghetu/actions/runs/34002080406)和 [Pages 部署](https://github.com/Rising1234Sun/qingmingshanghetu/actions/runs/34002080403)均已成功。
- 核对远端 110 个文件与本地 Git 对象一致，合计约 65.21 MiB；未上传视频、Blender 工程或原始 WAV。
- 22 个线上运行资产逐一通过 HTTP 和 SHA-256 核验。
- 实际打开线上网页，三维场景、中文界面、虹桥标签和简介图片显示正常，浏览器没有错误日志。
- 完整 OTF 字库与原始长卷保留在本地并改为按需下载；已经从公开来源下载到独立目录，大小与 SHA-256 均与原项目一致。

初次部署时，新建环境默认允许 `master`；已为本项目配置 `main` 部署分支。早期失败的工作流记录保留，后续成功记录见上述链接。

README 已补充当前项目不足，以及作者在抖音发布后反馈的视频模糊、iPhone 15 Pro Max 经剪映处理后的边缘裁切问题；这些问题明确标为尚未解决。
